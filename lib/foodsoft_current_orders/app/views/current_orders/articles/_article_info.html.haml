%span#article_info{style: 'margin-left: 1em'}
  - if order_article
    #{order_article.units_to_order} #{pkg_helper(order_article.price) if order_article.units_to_order>0} ordered,
    - if order_article.units_received
      #{order_article.units_received} #{pkg_helper(order_article.price) if order_articles.units_received>0} received,
    = Article.human_attribute_name(:fc_price)
    = number_to_currency(order_article.price.fc_price)
