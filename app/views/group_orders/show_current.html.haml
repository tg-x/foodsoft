- title 'My current order', false

- content_for :sidebar do
  = render 'edit_sidebar'

#articles_table
  .page-header
    %h1
      %i.icon-shopping-cart{style: 'color: #999'}
      My current order
      %span{style: 'font-weight: normal; font-size: 12pt; vertical-align: middle'}
        = orders_status_line(@orders)
  - if @order_articles and !@order_articles.empty?
    = render 'order_articles'
  - else
    You haven't ordered anything yet. Select one of the categories to get started.

