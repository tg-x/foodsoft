- title t('.title')

%p= link_to t('.new'), new_article_category_path, class: 'btn btn-primary'

%ol.sortable_tree{ data: { rebuild_url: rebuild_article_categories_url } }
  = sortable_tree collection, title: :name
