// update any input-delta fields (see GroupOrderArticlesHelper#group_order_article_edit_result)
// this is done client-side as well, this is to make sure client is up-to-date
$('.delta-input input#r_<%= @group_order_article.id %>').val(<%= @group_order_article.result %>);

// and trigger hooks for views including this
$(document).trigger({
  type: 'GroupOrderArticle#update',
  order_article_id: <%= @group_order_article.order_article_id %>,
  group_order_article_id: <%= @group_order_article.id %>
});
